import * as actionsTypes from "../Types/actionsTypes";

export const addArticle = (article: IArticle, action: ArticleAction) => {
  switch (action.type) {
    case actionsTypes.ADD_ARTICLE: {
    }
  }
};
